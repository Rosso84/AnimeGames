
/**This page is not in use yet */

/* 	
const mysql = require('mysql');

const config = {
    host: 'localhost',
    user: 'your user name',
    password: 'password',
    database: 'example schema name',
};
 */

/*
 * For efficiency, we're going to create a MySQL pool,
 * which allows us to use multiple connections at once 
 * instead of having to manually open and close multiple connections. 
 * */

/* const pool = mysql.createPool(config);



module.exports = pool; */